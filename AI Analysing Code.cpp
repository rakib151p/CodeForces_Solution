#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc=1;
    cin>>tc;
    while(tc--)
    {
        string s;
        cin>>s;
        cout<<s<<endl;
     /*   unsigned long long int n,x;
        cin>>n>>x;
        vector<long long>v;
        for(long long i=1; i*i<=n; i++)
        {
            if(n%i==0)
            {
                v.push_back(i);
                if(i!=n/i)
                {
                    v.push_back(n/i);
                }

            }
        }
        sort(v.begin(),v.end());
        if(v.size()<x)cout<<-1<<endl;
        else cout<<v[x-1]<<endl;*/
    }
}
